{
  "name": "full-aws-devops-app",
  "version": "1.0.0",
  "description": "Sample app for AWS DevOps pipeline demonstration",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest --coverage",
    "test:integration": "jest --testPathPattern=tests/integration",
    "lint": "eslint src/"
  },
  "dependencies": {
    "aws-sdk": "^2.1500.0",
    "aws-xray-sdk": "^3.5.0",
    "express": "^4.18.0"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "supertest": "^6.0.0"
  }
}
